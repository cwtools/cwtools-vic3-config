enums = {
    enum[duration] = {
        days
        months
        years
    }

    enum[character_role] = {
        politician
        general
        admiral
    }

    enum[character_trait_type] = {
        skill
        personality
        condition
    }
}