types = {
    type[scripted_list] = {
        path = "game/common/scripted_lists"
        path_extension = .txt
    }
}

scripted_list = {
    base = enum[scripted_list_base]
    conditions = single_alias_right[trigger_clause]
}
