types = {
    type[label] = {
        path = "game/common/labels"
        file_extension = .txt
    }
}

label = {
    type = enum[label_types]
    modifier_key = enum[label_modifier_keys]
    modifier = single_alias_right[modifier_clause]
}